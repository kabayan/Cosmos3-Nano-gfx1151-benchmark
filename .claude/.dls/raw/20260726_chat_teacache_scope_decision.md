# ユーザー指示記録: 計算省略系の扱いと TeaCache 品質評価（2026-07-26）

- 文脈: `/dls-commit` 実行時の引数として、調査レポート
  `docs/cosmos3_rocm_further_speedup_reassessment_20260726.md` §5 推奨アクション
  （③ TeaCache 品質検証付き導入 / ⑤ SageAttention INT8）への判断が示された。

## ユーザー指示（逐語）

> 計算系省略は元記事と同じ環境での比較が必須なので不可 それ以外TeaCacheでどの程度異なるかを知りたい

## 解釈

1. TeaCache 等の計算省略（近似キャッシュ）系は、「元記事（論文値）と同じ計算内容での
   比較」というプロジェクトの本線においては **不可**（採用しない）。
2. ただし本線とは別に、**TeaCache を適用すると出力がどの程度異なるか**（品質差の定量）
   は知りたい → 品質差評価タスクとして実施する。速度更新の本線記録には含めない。
